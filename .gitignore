assets/partners/page-*.html
__pycache__/
*.pyc
.venv/
backend/.env

# Depth cache: our derived scores keyed by lyrics hash — never lyric text.
backend/data/

# Uploaded freestyle audio + async job store (our pipeline I/O; never commit)
uploads/
backend/data/jobs.json
